<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gts.base.platform">

<insert id="insertTest" parameterType="com.gts.base.platform.dao.entity.Test_table" keyProperty="id" useGeneratedKeys="true">
	INSERT INTO test (ID,NAME)VALUES(#{id,jdbcType=VARCHAR},#{name,jdbcType=VARCHAR})
</insert>

<select id="listPageTest" resultType="com.gts.base.platform.dao.entity.Test_table" parameterType="com.gts.base.platform.dao.entity.Test_table" flushCache="true" >
	<![CDATA[
		SELECT ID,NAME as userId FROM test WHERE 1=1 
	]]>
	<if test="id != null and id !=''">
		AND ID=#{id}
	</if>
	<if test="name != null and name!=''">
		AND NAME LIKE '%${name}%'
	</if>
	ORDER BY ID DESC
</select>
	
</mapper>
